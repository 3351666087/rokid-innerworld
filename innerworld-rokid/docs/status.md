# Status

## 2026-07-04

- Target observation diagnostics checkpoint at 14:10 Asia/Shanghai: Unity now emits stable `IW_TARGET_*` log tokens for Rokid image target events, unknown image indexes, live-pairing/session gates, throttling, POST start/result/failure, and trusted mission-assist outcomes. `field:live-pass:watch --logcat` counts these tokens without writing raw logcat, pairing codes, session ids, serials, private IPs, or MAC addresses.
- Verified after the diagnostics change: `git diff --check`, `npm run check:unity`, `npm run check:contract`, `npm run check:field-live-pass`, `npm run check:field-target-pass`, `npm run check:mainline`, and `npm run context:export`. Latest live-pass still shows one operator-paired live session but trusted A1/A2/A3, mission loop, User B readback, and field acceptance remain false.
- Field acceptance session runner added and validated at 13:06 Asia/Shanghai: `tools/field-acceptance-session.ps1` and npm scripts `field:acceptance-session`, `field:acceptance-session:live`, and `field:acceptance-session:strict` now bundle real-device prechecks, endpoint snapshots, Station Pro pair-smoke when explicitly requested, and live-pass watching into one sanitized field evidence report. Default mode is non-mutating and does not post simulator/manual observations or mission/write-back state.
- Latest `npm run field:acceptance-session` pass is green for the precheck wrapper: strict device probe OK, APK inspect OK, one operator-paired live session ready, and `hardware_ready_claim_allowed=false`. The next physical actions remain A1 scan, A2/A3 re-bind through the operator-paired live SDK session, A2 read, A3 write-back, and User B readback.
- Strict live-pass still blocks as expected with `trusted_a1_a2_a3_observations_missing` and `p0_mission_writeback_user_b_loop_missing`; this confirms the new runner did not weaken the hardware-ready boundary.
- Carver short diff audit found the runner direction/privacy OK and flagged strict exit-code semantics. The runner now propagates `field-live-pass` JSON `ok=false` into the session command/result, so `-RequireTrusted -RequireMissionLoop` exits non-zero until real physical evidence exists.
- Field live-pass watch checkpoint at 12:41 Asia/Shanghai: `tools/field-live-pass.js` now reports `hardware_a1_a2_a3_ready`, `missing_trusted_anchor_ids`, `missing_hardware_anchor_ids`, `missing_mission_step_ids`, and `next_required_actions`. This keeps the tool read-only while giving the field operator exact next steps during the physical A1/A2/A3/User B pass.
- Latest live-pass baseline is more specific: one operator-paired live session is online, trusted anchors are still `0/3`, raw hardware alignment is missing `A1`, and A2/A3 currently need re-scan or re-bind through the operator-paired live SDK session because their existing hardware-mode observations are not trusted. Hardware-ready remains false.
- Real-device probe hardening checkpoint at 12:31 Asia/Shanghai: `tools/device-probe.ps1` now wraps ADB/tool version calls and Windows PnP enumeration with bounded timeouts, records `timeouts.command_seconds`, `timeouts.pnp_seconds`, `adb_timed_out`, and `pnp_timed_out`, and keeps the same sanitized privacy boundary. `server/space-server/check-device-probe.js` now has an outer 90s PowerShell timeout and asserts the timeout schema so `npm run check:device-probe` cannot hang indefinitely.
- Carver completed a fresh source/PDF audit for this checkpoint at `.agents/carver/carver-source-audit-2026-07-04-1231.md`. It confirms the group/teammate materials still converge on the P0 one-wall Rokid loop, and that 19 target PDFs were readable through PyMuPDF text extraction. The audit repeats that hardware-ready remains unproven until the real A1/A2/A3/User B loop passes under trusted live SDK evidence.
- Carver diff audit for this checkpoint is `.agents/carver/carver-diff-audit-2026-07-04-1234.md`; recommendation is `push`, with no P0 drift, no privacy-boundary expansion, and no evidence-pointer blocker found.
- Latest strict device probe is green: one sanitized Station Pro-class USB ADB device is in `device` state, Android SDK/Unity/Java/Maven/Node are visible, PnP summary has 12 sanitized devices, and there are no warnings. This is still connected-device evidence, not hardware acceptance.
- Station APK evidence split fix: after a mutating `station:apk:pair-smoke`, `check:station-apk:rkimage` now reads `station-pro-apk-smoke-latest-inspect.*` instead of the generic latest pointer, and asserts `evidence_kind=inspect_only`. This prevents non-mutating package gates from accidentally consuming install/launch evidence.
- LAN Space Server was restarted on `0.0.0.0:5177`; `npm run station:apk:pair-smoke` passed again for current APK SHA `bd852f7012e25f9ccd2630e2113a1a3526fc7bdfea5d05c32d56c410303fe142`; `npm run check:field-live-pass` is green for one online operator-paired live session while trusted A1/A2/A3, mission loop, User B readback, and field acceptance remain false. The strict command `node tools/field-live-pass.js --single --require-trusted --require-mission-loop` still blocks as expected.
- Verified after this hardening: `npm run check:device-probe`, `npm run device:probe:strict`, `npm run station:apk:inspect`, `npm run check:station-apk:rkimage`, `npm run check:mainline`, `npm run check:contract`, and `npm run check:field-live-pass`.
- PR #1 follow-up checkpoint: Carver reviewed commit `1358664f` as directionally safe for the P0 real-device lane, with the next work locked to physical A1/A2/A3 observations, A3 TimeMark write-back, User B readback, and field acceptance.
- Field acceptance now requires explicit User B readback. The `mission_loop` gate requires `user_b_readback`, exposes `active_user`, `required_active_user=B`, and `user_b_readback_ready`; completed mission steps plus an A3 write-back beacon are not enough unless `/api/state.active_user` is `B`.
- `tools/field-live-pass.js` now includes `user_b_readback_ready` in `mission_loop_ready`; the strict negative command `node tools/field-live-pass.js --single --require-trusted --require-mission-loop` blocks as expected with `trusted_a1_a2_a3_observations_missing` and `p0_mission_writeback_user_b_loop_missing`.
- LAN Space Server was restarted onto the current code, `npm run station:apk:pair-smoke` passed again on the connected Station Pro for current APK SHA `bd852f7012e25f9ccd2630e2113a1a3526fc7bdfea5d05c32d56c410303fe142`, and `npm run check:field-live-pass` is green for the live operator-paired session window while still showing trusted A1/A2/A3, User B readback, and field acceptance as false.
- Verified after the change: `npm run check:field-acceptance`, `npm run check:field-acceptance -- --api`, `npm run check:unity`, `npm run check:contract`, `npm run check:field-live-pass`, and the expected-blocker strict field live pass command.

## 2026-07-03

- Station Pro native-lib checkpoint passed at 23:14-23:17 Asia/Shanghai. Current APK: `output/unity-android/InnerWorldRokid.apk`, 45,719,155 bytes, SHA256 `bd852f7012e25f9ccd2630e2113a1a3526fc7bdfea5d05c32d56c410303fe142`.
- `tools/build-unity-android.ps1` now treats missing Rokid native libraries as a build failure. If Unity batchmode creates an APK that lacks `librokid_openxr_api.so` / `libyuv.so`, it automatically reassembles the Unity-generated Gradle project after staging those libraries from the Rokid OpenXR AAR into generated `jniLibs`.
- APK inspect and release gate now require both `assets/RKImage.db` and Rokid/OpenXR native libraries. `npm run station:apk:inspect` and `npm run check:station-apk -- --skip-smoke --require-lan --require-rkimage-db --require-rokid-native-libs` passed with `libopenxr_loader.so`, `librokid_openxr_api.so`, and `libyuv.so` packaged for `arm64-v8a`.
- `npm run station:apk:pair-smoke` passed twice on the connected Station Pro for the current APK: install OK, cold launch OK, process observed, `is_uxr_app=true`, operator pairing issue/injection/verification OK, and no raw pairing code persisted in evidence.
- After clearing all logcat buffers and relaunching the current APK, sanitized pattern counts were `DllNotFoundException=0`, `rokid_openxr_api=0`, `UnsatisfiedLinkError=0`; the previous `rokid_openxr_api` blocker is resolved for this build.
- `npm run check:uxr-readiness:ready` passed. Runtime field acceptance is still correctly `rehearsal_ready`, not hardware-ready: trusted hardware observations remain `0/3`, hardware alignment still lacks trusted A1/A2/A3 evidence, and the P0 mission/write-back loop has not been completed on the live device.
- Unity image tracking path is ready for the next physical wall pass: Rokid image index `1/2/3` maps to `A1/A2/A3`; A1 posts `qr`, A2/A3 post `image_tracking`; posts are gated by an operator-paired live SDK session before they can count as trusted hardware evidence.
- Added read-only现场 live monitor `tools/field-live-pass.js` with npm scripts `field:live-pass`, `field:live-pass:watch`, and `check:field-live-pass`. Current snapshot proves the live operator-paired session window is open (`live_session_ready=true`, one online live operator-paired session) while correctly showing `trusted_a1_a2_a3_ready=false`, `mission_loop_ready=false`, and `field_acceptance_ready=false`.
- Negative acceptance command is wired: `npm run field:live-pass -- --single --require-trusted` fails with `trusted_a1_a2_a3_observations_missing`, without raw session ids or raw logcat. Use this during the physical target pass to avoid accidentally counting rehearsal evidence.
- C drive was checked and is about 17.5GB free. No broad cleanup was performed; future cleanup must stay inside project-generated Rokid outputs/caches or guarded scripts only.

- Evidence-chain reconciliation completed at 22:18 Asia/Shanghai. The current disk APK is `output/unity-android/InnerWorldRokid.apk` at 45,167,409 bytes with SHA256 `ce3f118632d6202c61455f19cdc11080a765e17c74f6be07496ef2c2b571cf1d`.
- `tools/build-unity-android.ps1` now records final APK size/SHA after post-checks. A non-rebuild refresh command, `powershell -File tools/build-unity-android.ps1 -SkipUnityBuild -RunPostChecks -RequirePostCheckDevice`, updated `unity-build-android-latest.*` as `existing_apk_only`; it did not install or launch the APK.
- Current non-mutating evidence is green: `npm run check:station-apk:lan`, `npm run uxr:doctor`, and `npm run check:uxr-readiness:ready` passed. The current APK inspect evidence matches SHA `ce3f118632d6...`, is `private_lan`, has `network_ready_for_device=true`, and keeps UXR manifest markers.
- Boundary correction: the last mutating launch evidence is still valid only for previous APK SHA `70592a5dfec4...`, not for the current `ce3f118632d6...` APK. `uxr-readiness-latest` now warns `latest_mutating_launch_apk_sha_mismatch_current_apk` and sets the next required proof to `user_confirmed_current_apk_station_pro_install_launch_smoke`.
- Hardware-ready remains false. No new current-APK install/launch, operator-paired live SDK binding, trusted A1/A2/A3 observations, A3 write-back, User B readback, or `/api/field/acceptance` pass was performed in this reconciliation checkpoint.

- Station Pro UXR manifest/install/run checkpoint passed at 21:05 Asia/Shanghai. The rebuilt APK is `output/unity-android/InnerWorldRokid.apk` (24,753,149 bytes, SHA256 `f4c1a909cbcd11af98d1ca9dd53a2ca7ada1248a60c9c78e12b718786ca19884`). It embeds LAN config, `com.rokid.sdk=uxr`, `com.rokid.uxr.application.mode=3d`, OpenXR/UXR package queries, camera/audio permissions, and `uxr_manifest_ready=true`.
- `npm run station:apk:smoke` now installs and launches on the connected Station Pro: install OK, `am start` status OK, process observed, `is_uxr_app=true`, and no launch error. This supersedes the earlier error-102 blocker for the plain Android package.
- Space API live heartbeat is now proven over LAN from the running Unity app: `/api/device/sessions` shows one online `unity-shell-rokid-ar` session with heartbeat count increasing, health `ok`, active anchor `A3`, and pose present.
- Boundary remains strict: this is install/run plus LAN heartbeat, not official Rokid SDK live binding. The session still reports `sdk_binding_status.stage=fallback_only`, `live_binding_ready=false`, pairing is unpaired, and `hardware_acceptance_eligible=false`; RKCameraRig/RKInput/PointableUI/image target/SLAM trusted A1-A3 evidence remain P0.
- Fixed `tools/station-pro-apk-smoke.ps1` so launch diagnostics are queried after install, avoiding stale `isUxrApp=false` from the previously installed APK.
- Hardened smoke/readiness evidence at 21:19 Asia/Shanghai: `station-pro-apk-smoke` now writes separate latest inspect and latest mutating-launch pointers, and `check:uxr-readiness` no longer requires the superseded error-102 blocker when the latest mutating launch passed.

- Superseded 20:48 blocker audit: `.agents/carver/uxr-blocker-audit-2026-07-03.md` correctly identified Station Pro UXR launch acceptance as the blocker; the 21:05 UXR manifest smoke resolved that specific install/run blocker.
- Earlier 20:48 smoke history: the previous Gradle LAN APK installed but did not launch, with `is_uxr_app=false` and error `102`. Keep this only as regression context for why `uxr_manifest_ready` is now a hard APK check.
- Current boundary is strict: package/LAN/install/run/heartbeat evidence exists, but hardware-ready remains false. Missing proof: official UXR/OpenXR package validation, RKCameraRig/RKInput/PointableUI binding, trusted A1/A2/A3 QR/image_tracking/SLAM observations, operator pairing, and `/api/field/acceptance`.
- Next P0 action: keep the UXR manifest gate, then install `com.rokid.xr.unity`, run Environment Fix / OpenXR Feature Groups / Project Validation, and bind the live Rokid SDK adapters so heartbeat stage can move beyond `fallback_only`.
- C drive is still tight before UXR import/build work: guarded temp cleanup removed one old Visual Studio installer temp item (~0.121GB) and left C at about 23.77GB free. Preserve Unity/Android caches; keep monitoring before another large Unity/Gradle/UXR operation.

- Direct LAN Gradle APK rebuild completed at 20:15-20:18 Asia/Shanghai. The generated Unity Gradle project was patched with Aliyun Maven mirrors and `:launcher:assembleRelease` succeeded; `launcher-release.apk` was copied to `output/unity-android/InnerWorldRokid.apk` (24,752,749 bytes). This supersedes the earlier config-only patched APK as the current standard output artifact.
- Latest non-mutating APK evidence now passes as LAN-ready: `npm run station:apk:inspect` reports package `com.innerworld.rokid.prototype`, launchable `com.unity3d.player.UnityPlayerGameActivity`, INTERNET permission, cleartext/network security enabled, `config_host_kind=private_lan`, `network_ready_for_device=true`, and `space_id=innerworld_campus_wall`. `npm run check:station-apk:lan`, `npm run device:probe`, `npm run check:device-probe`, and `npm run check:mainline` also passed.
- `npm run field:preflight -- -RequireLan` passed after the rebuild and regenerated the field kit/config. The Unity source config host hash matches the embedded APK config host hash, so the field kit and APK are aligned without printing raw private IPs.
- No Station Pro install or launch was performed in this 20:18 checkpoint. The result proves LAN-ready package/config evidence only; live heartbeat, official UXR/OpenXR binding, trusted A1/A2/A3 observations, and `/api/field/acceptance` remain pending.
- User preference recorded: if Gradle/Maven dependency downloads fail again, use IDM (`IDMan.exe`) to fetch failed URLs into a controlled local Maven/Gradle cache. This pass did not require IDM because the mirror patch cleared the dependency failures. Follow-up hardening: move mirror/IDM fallback out of generated `Library/Bee/.../Gradle/settings.gradle` so a fresh Unity export does not erase it.
- C drive free space after this checkpoint is about 26GB. Continue guarded cache/temp cleanup only; keep useful Unity/Android build caches for the next hardware pass.

- Real-device LAN APK checkpoint advanced at 20:12-20:15 Asia/Shanghai. The Space Server was restarted in LAN mode on `0.0.0.0:5177`; `npm run field:preflight -- -RequireLan -SkipPdf -SkipCacheReport` passed and updated Unity `StreamingAssets` / `Resources` config to a private-LAN URL while reports/stdout redact the IP.
- Added privacy-safe LAN gates: `npm run check:station-apk:lan` now fails if the APK config is still `localhost`, and `field-preflight` / `unity:config` no longer print raw private IPs in evidence output. Latest privacy scans for field preflight and APK patch reports found no raw private IP or raw identifier keys.
- Unity Android batch rebuild reached Gradle but failed on external dependency TLS handshakes. To keep the hardware lane moving, `npm run station:apk:patch-lan` now performs a config-only APK patch: replace `assets/innerworld-config.json`, remove old signatures, zipalign, debug-sign, and verify v2 signature. This produced `output/unity-android/InnerWorldRokid.apk` with SHA256 `3c28976481e2cc742e5dca5e1ca9dbf94beab0da00aec5469e2b6499ae822101`, `config_host_kind=private_lan`, and `network_ready_for_device=true`.
- `npm run station:apk:smoke` was run against the connected Station Pro. ADB install succeeded and the launch intent was attempted, but Android refused to start the activity with error code `102`; diagnostics show `is_uxr_app=false` and `can_launch_by_default=false`. This proves install path and Station policy boundary, not install/run smoke, live heartbeat, UXR binding, or field acceptance.
- Next blocker is now concrete: the app must become a real Rokid UXR/AR Studio app through official UXR/OpenXR package validation and manifest/runtime binding. Plain Unity Android fallback APKs can be installed but cannot be launched as the Station Pro spatial runtime.

- Fresh Carver re-audit completed for the user's "read every teammate upload again" request. Carver's write set is `.agents/carver/`; the final report is `.agents/carver/source-audit-2026-07-03.md`. It rendered `docs/策划案.pdf` into 13 page PNGs under `.agents/carver/pdf-render/`, smoke-rendered 19 raw PDFs under `.agents/carver/pdf-render-smoke/`, and confirmed the root proposal PDF is readable with `pypdf`/`pdfplumber`.
- Carver found that old `analysis/extracted_attachments/**/pdfinfo.txt` files are mostly broken Poppler-wrapper output, not trustworthy page metadata. Future PDF metadata should use `pages.json` or the real Poppler executables under `C:\Users\33516\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\poppler\Library\bin`.
- Root attachment extraction tooling was fixed: `tools/extract_attachments.py` now prefers the real bundled Poppler executables and writes `pdfinfo.txt` only when `pdfinfo` succeeds. Verified against `docs/策划案.pdf`: page count is 13 through the corrected path.
- Real-device evidence was rerun at 19:41 Asia/Shanghai. `npm run device:probe`, `npm run station:apk:inspect`, `npm run check:device-probe`, `npm run check:station-apk`, and `npm run check:mainline` passed. Latest probe evidence still shows one sanitized `RG_stationPro` / `stationPro` USB ADB device in `device` state, ADB 36.0.0, Android SDK build-tools/platforms, Unity Editor `6000.3.19f1`, Java/Maven/Node/npm, and no warnings from `check:device-probe`.
- GitHub was fetched and checked with `gh`: local `main`, `origin/main`, and `origin/HEAD` remain at `b21fa314`; `gh pr list` and `gh issue list` returned empty arrays for `3351666087/rokid-innerworld`. The only teammate GitHub commit found on main remains Shiyao Zhang's `f402f82f61d62e897d7615fa3f4259423e5cfce9`.
- Carver's P0 task queue is now: confirm Rokid/Unity/UXR access and hardware popups, keep sanitized probes running, replace APK `localhost` with LAN config, rebuild Android/Rokid APK, run Station Pro install/launch smoke, install official UXR, bind RKCameraRig/RKInput/PointableUI, import A1/A2/A3 target library, then prove operator-paired heartbeat and trusted `/api/field/acceptance`.
- C drive free space was checked, then `npm run cache:temp:clean` removed about 1.39GB of old Visual Studio installer temp files. C now has about 30.3GB free. One larger temp candidate was skipped because Windows reported a file lock; Unity and Android build caches were kept.
- Carver completed the full source review requested for the real-device phase. The durable local record is `docs/carver-source-review.md`: group PDFs are unpacked/readable through `text.txt` and page artifacts, P0/P1/P2 boundaries are restated, and the next work is explicitly narrowed to `station_pro_trusted_hardware_session`.
- Non-mutating Station Pro APK inspection now passes after the config parsing fix. `npm run station:apk:inspect` and `npm run check:station-apk` read the APK package/activity/manifest/config and correctly report `config_host_kind=localhost`, `network_ready_for_device=false`, one connected Station Pro-class ADB device, and `install_and_launch=false`. This is package evidence, not live heartbeat or hardware acceptance.
- Carver is the active long-line mainline reviewer sub-agent name for future checkpoint reviews. Historical notes may still mention Kepler, but new status and bus entries should use Carver.
- Local/GitHub alignment checked after `git fetch --prune origin`: local `main`, `origin/main`, and `origin/HEAD` are all at `b21fa314` (`Adopt teammate docs bus for hardware phase`). The teammate GitHub commit is `f402f82f61d62e897d7615fa3f4259423e5cfce9` by Shiyao Zhang and only adds the design/SDK/PDF reference files now governed by `docs/teammate-docs-bus.md`.
- Group-chat final-plan files were re-read from `analysis/extracted_attachments`: the 147 decision table, 170 latest group integration, 193/249 proposal lineage, and 312 hybrid Spatial Pin path all support the same P0 lock: a controlled campus memory wall / small-space hidden layer with read -> task/service action -> A3 write-back -> User B readback. Broad campus routing, open social UGC, dashboards, and platform scope stay out of the hardware lane.
- Strict local hardware probe passed at `2026-07-03T19:12:30+08:00`: sanitized ADB evidence sees one connected Station Pro-class USB device in `device` state with model `RG_stationPro` / device `stationPro`; ADB 36.0.0 is available under `C:\Program Files (x86)\Android\android-sdk\platform-tools\adb.exe`; Android SDK build-tools `36.0.0`, platforms `android-35`/`android-36`, Node, npm, Java, Maven, Unity Hub, and Unity Editor `6000.3.19f1` are present.
- Environment gaps remain explicit: `adb` is not currently on PATH, Unity is not on PATH, `ANDROID_HOME`/`ANDROID_SDK_ROOT`/`JAVA_HOME` are unset in the current shell, AR Studio / official Rokid UXR package access is not yet verified, and no live Unity/UXR APK heartbeat or trusted A1/A2/A3 hardware observation has passed. This is connected hardware, not hardware-ready acceptance.
- C drive free space was checked at roughly 23.5GB. Continue using guarded temp/cache reports and keep Unity/Android build caches only when they are useful for the next real-device pass.
- Carver final-reviewed the teammate-docs bus checkpoint and cleared it for commit/push. The remaining P0 instruction is to keep driving `station_pro_trusted_hardware_session`: UXR3.0 validation, APK install/run, live heartbeat, then operator-paired trusted A1/A2/A3 observations and `/api/field/acceptance`.
- Carver re-reviewed teammate commit `f402f82f61d62e897d7615fa3f4259423e5cfce9` for real-device adoption. The result is now recorded in `docs/teammate-docs-bus.md`: P0 merges UXR3.0 validation, RKCameraRig, RKInput 3DoF ray, PointableUI, A2/A3 image target library, SLAM/head tracking heartbeat, operator-paired live SDK proof, and trusted calibration observations; P1 keeps near/far layout, bounded gestures, controlled audio, and controlled TimeMark authoring; P2/reference keeps institution backend, social/platform features, broad routes, spatial drawing, and dashboards out of the current hardware lane.
- The 13-page teammate PDF is treated as evidence for Campus Hidden Layer / Spatial URL / fixed-place TimeMark / AI semantic compression / one-place demo, not as permission to pivot into account/social/platform scope during P0.
- Real-device phase started. The Windows development machine now sees a connected Rokid Station Pro over USB ADB as model `RG-stationPro` / device `stationPro`, Android 12, with an ADB interface and `device` state.
- Full serial numbers, USB instance ids, MAC addresses, private IPs, and pairing codes are treated as private hardware identifiers. They must not be committed to docs, release evidence, public JSON, screenshots, or GitHub.
- `station_pro_trusted_hardware_session` is the new P0 checkpoint. The immediate path is: sanitized device/toolchain probe -> operator pairing -> install/run Unity or AR Studio APK on Station Pro -> live heartbeat -> trusted A1/A2/A3 observations -> field acceptance and evidence replay.
- A second connected glasses line identified as Rokid x Bolon remains a secondary research lane. It does not replace the Max Pro + Station Pro AR Studio mainline unless it proves the same UXR/RKCameraRig/RKInput/image target/SLAM development path.
- Current evidence is enough to begin true hardware integration work, but it is not yet hardware-ready acceptance. Simulator/manual/fallback evidence still remains rehearsal only.

## 2026-07-02

- 长期目标保持 active：在 `C:\Users\33516\Documents\Rokid` 内用本机完整权限持续推进 Rokid「镜见 InnerWorld / 校园记忆展墙」。
- Windows 开发环境可用：Unity Hub、Unity Editor `6000.3.19f1`、Android SDK/NDK/OpenJDK、Node、Maven `3.9.16` 已安装。
- 本地服务统一为 `http://localhost:5177`；LAN 模式用 `npm run dev:lan` 绑定 `0.0.0.0`，供 Rokid/手机访问 Windows 主控机 IP。
- Web demo 已通过 Chrome 插件完成可视彩排，并在本轮重新验证：点击「快速彩排」后最终状态为 `User B`、`complete`、写回点显示「后来者看见了新信标」，log 中 `current_step` 为 `write_back`。
- 2026-07-02 11:24 Chrome 插件复验结果：`/api/state` 显示 `active_user=B`、`mission_state=complete`、`current_step_index=3`、`completed_steps=[read, find_year, service_action, write_back]`、`beacons=3`；最新写回信标为 `B_WRITE_1782962678108`，锚点 `A3`，写回时间 `2026-07-02T03:24:38.108Z`。
- Space Server 已支持 CORS、`OPTIONS` preflight、`/api/health`、no-store JSON，并通过 `npm run check:unity`。
- Unity shell 支持可配置 API 地址：命令行 `--innerworld-api=...`、环境变量 `INNERWORLD_API_BASE_URL`、持久化配置、`StreamingAssets`、`Resources` 包内配置都可用。
- Windows fallback 已重建：`output\unity-windows\InnerWorldRokid.exe`，并复制 `InnerWorldRokid_Data\StreamingAssets\innerworld-config.json`。
- Windows Unity fallback 已用 Windows 应用控制实机窗口验收：HUD 显示 `LOCALHOST LIVE`，A1/A2/A3 三锚点可见，热键提示可读；按 `Space, Space, S, W, B, R` 后窗口和 API 都进入 `active_user=B`、`mission_state=complete`、`completed_steps=4`、`beacon_count=3`。
- 修复 Unity Player 粉墙/黑场问题：不再用 `Shader.Find("Standard")` 直接创建运行时材质，改为跨 Built-in/URP 的可用 shader 查找和 renderer 现有材质兜底；Player.log 已无材质空引用异常。
- 修复 Space Server 完成态归一：所有任务完成或包含 `write_back` 时强制 `mission_state=complete`，避免客户端切 User B 时把状态降回 `entered`。
- Android fallback APK 已重建：`output\unity-android\InnerWorldRokid.apk`，生成时间 `2026-07-02 11:19:30`，SHA256 `D507E209A245CB663FF775E85B098AF159DE977E642B45606DA1F8BE35285574`。
- Android APK 已用 `aapt` 验证：manifest 中包含 `android.permission.INTERNET`、`android:usesCleartextTraffic=true`、`android:networkSecurityConfig=@xml/...`。
- Android 配置兜底已验证：APK 内包含 `assets/innerworld-config.json`，Unity `Resources` 配置也被打入 `assets/bin/Data/cb7d38ef34a54f68b7f0196e2b07d22a`。
- `tools/set-unity-config.ps1` 会同时更新 `Assets\StreamingAssets\innerworld-config.json` 和 `Assets\Resources\innerworld-config.json`，避免 LAN IP 配置分叉。
- 交付包已重新生成并验包：包含 Android 插件、Resources 配置、StreamingAssets 配置、Windows fallback、Android APK、文档和缓存报告，禁入目录计数为 0。
- 新增 `npm run evidence:rehearsal`：自动走完 reset、读取、寻找年份、加入活动、写回、切 User B，并把 JSON/Markdown 验收证据写入 `output/demo`。带 `-- --reset-after` 时保存证据后恢复初始态。
- `npm run evidence:rehearsal -- --reset-after` 已验证通过，最新证据写入 `output\demo\rehearsal-evidence-latest.json` / `.md`：最终态为 `active_user=B`、`mission_state=complete`、`completed_steps=4`、`beacon_count=3`，随后 reset 回 `entered`、2 个信标、0 个完成步骤。
- 打包脚本已排除 `data/runtime_state.json`，避免把 Chrome/Unity 彩排后的 `complete` 运行态带进交付包；解包运行烟测已验证：包内 server 首次启动会自动生成运行态，`/api/health` 返回 `entered`、2 个信标、0 个完成步骤。
- 新增并验证 `npm run pdf:fieldkit`：通过 sibling `pdf-renderer` 的 Java/OpenHTMLToPDF 渲染可打印现场包 `output\pdf\rokid_innerworld_field_kit.pdf`，内容包含入口 QR、设备页、90 秒流程、A1/A2/A3 锚点牌、操作员清单和自动彩排证据摘要。`pdfinfo` 显示 7 页 A4；已用 Poppler 渲染 PNG 目检封面、启动页、锚点牌和操作员页，无文字重叠或中文缺字。
- 新增 `npm run server:package` 和 `docs\server-deploy.md`：生成轻量 Space Server 发布包，包含 Node server、Web demo、`space_demo.json`、AI schema/prompt、文档和 Windows/Linux 启动脚本；明确排除 `data/runtime_state.json`、Unity 输出、`node_modules`、`.git` 和缓存，用于先本地解包烟测，再上传小主机。
- 新增 `npm run server:smoke`、`npm run package:audit`、`npm run verify:release`：把 server-only 解包烟测、主包/内嵌 server release SHA 校验、必需文件检查和禁入目录检查固化为脚本，减少现场前手工验包失误。
- 新增 `npm run server:deploy-plan`：根据最新 server-only zip/manifest 生成 `output\server-release\deploy-plan-latest.md` / `.json`，包含上传路径、Linux 启动、systemd、Caddy 和 Nginx 反代示例；只写本地计划，不连接服务器。
- 新增 `npm run env:doctor`：汇总 Node/npm/Java/Maven/Unity/Android 模块、磁盘、端口、API health、Windows EXE、Android APK、PDF、最新主包和 server-only 包，证据写入 `output\env-doctor`，并接入 `verify:release` 和主包审计。
- 新增 `npm run field:preflight`：自动识别 LAN IP、检查 localhost/LAN health、更新 Unity API 地址、按 LAN URL 重渲染现场 PDF，并把现场预检证据写入 `output\field-preflight`；LAN 不通时默认拒绝更新 Unity/PDF，正式现场可用 `-RequireLan` 强校验，主包审计会检查该脚本和 latest 报告存在。
- 新增 `npm run release:index`：汇总最新主包、server-only 包、EXE/APK/PDF、环境医生、现场预检和彩排证据，输出 `output\release-index\release-index-latest.md` / `.json`；`verify:release` 在主包审计和 post-package 环境医生后自动运行。
- `tools\render-field-kit-pdf.ps1` 已改为优先使用 `D:\Downloads\RokidCache\m2-repository` 作为 Maven 本地仓库；`cache:report` 新增 Maven C 盘仓库、Windows Temp 总量和 D Maven cache 报告项，便于持续观察 C 盘风险。
- 新增并试运行 `npm run cache:temp:report` / `npm run cache:temp:clean`：只处理用户 Temp 下 6 小时以前、可识别为 VS/Android/.NET 安装器残留且无进程引用的目录，并显式跳过 junction/symlink 等 reparse point；本轮删除 `glgyitip`、`15tbxetl`、`b54aud4l.eiq`，释放约 7.416GB。
- C 盘仍接近 25GB 警戒线，清理后约 23.741GB 可用；继续高频执行 `npm run cache:temp:report`、`npm run cache:report` / `npm run cache:clean`。不随意删除 Unity `Library`。

## Next

- 重新生成最终交付包并验包，确认服务器部署计划、环境医生、release index、现场 LAN 预检脚本、Windows/Android fallback、Space Server 状态机修复和文档全部进入 zip。
- 硬件到场后，把 Rokid AR Studio 输入/显示层接到同一套 Space API 和数据契约。
