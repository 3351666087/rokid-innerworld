# Status

## 2026-07-07

- Executable scene target checkpoint: A1/A2/A3/User B `scene_actions` now have `task_target` contracts with endpoint sequences, trusted-shiyao-scan requirement, and fallback no-hardware guards. Unity scene action nodes are clickable/gaze-selectable targets that call existing local confirm, interactions, service action, write-back, and User B readback paths. Web fallback cards can execute the same sequence through Space API while displaying `no local hardware claim` status.

- Shiyao-first spatial choreography checkpoint: `scene_actions` now include `spatial_choreography` for each P0 concrete task: time layer, depth meters, wall seed rule, gesture affordance, spatial sound, and three growth beats. Unity renders growth beat beads/trails from the same data, and the web fallback exposes time/depth/gesture/beats while keeping `data-hardware-ready="false"`. Contract and package audit now gate these fields so the post-scan scene cannot regress into flat cards.
- Shiyao-first checkpoint at 19:24 Asia/Shanghai: teammate `shiyao` now holds the real Rokid hardware and owns a branch with scan/logo/QR scene discovery. Current branch priority is no longer local hardware claim work; it is to make the concrete post-scan A1/A2/A3 scene useful, spatial, and merge-friendly. Added `scene_actions` to `data/space_demo.json` for A1 check-in stamp, A2 memory fragment visit, A3 TimeMark write-back, and User B readback, each with physical wall cue, spatial binding pose/projection/depth layer, and P0 role.
- External Opus design checkpoint: `claude-opus-4-8` was used as a high-value spatial/frontend design reviewer and saved local-only notes under `.agents/carver/`. Key design principle adopted: the wall is a seed, not a flat screen; content should grow from the wall along depth, and `Depth = time`. This remains a design aid only; no external API key or secret is written to repo.

## 2026-07-05

- Plan reset at 22:03 Asia/Shanghai: active development is explicitly aligned to PR #2 `docs/demo-plan.md` / `Campus Hidden Layer`. P0 remains the true Rokid one-wall A1 spatial entry, A2 memory read, A3 TimeMark write-back, and User B readback loop. `Whale Cloud Sky Pin` stays a controlled 3D Pin preview only and must not count as hardware/P0 acceptance evidence. Next execution lanes are real input recovery, spatial depth/product polish, demo package guards, field acceptance closure, and Carver long-line audit of teammate docs/PDFs and checkpoint diffs.
- Demo plan alignment checkpoint: imported PR #2 `docs/demo-plan.md` as the narrative wrapper for `Campus Hidden Layer`. It does not replace P0. The mainline remains one real wall, A1 entry, A2 memory read, A3 TimeMark write-back, User B readback, and Rokid real-device acceptance. `Whale Cloud Sky Pin` is controlled preview content only, not open UGC, merchant dashboards, guide scope, phone-page scope, broad routing, or a platform pivot.
- Carver audit note: use PR #2 as a source for `docs/demo-plan.md` only. Do not merge the PR branch wholesale because it can roll back current hardware-readiness, Unity XR, field scripts, checks, and docs.

Latest field operator plan CLI checkpoint: `field:operator-plan` now snapshots read-only `/api/field/operator-plan` into sanitized JSON/Markdown under `output/field-operator-plan/field-operator-plan-latest.*`. It reports current phase, readiness, next actions, phase table, source-of-truth endpoints, P0 scope guard, and blockers, without running ADB/logcat, creating simulator/manual observations, mutating mission state, writing field evidence, or including raw serial/session/device/private-IP/pairing/pose/logcat/dumpsys data.

Latest field acceptance wrapper checkpoint: `field:acceptance-session` now captures `field:operator-plan` inside the same鐜板満 session report. The session JSON/Markdown includes `operator_plan.current_phase`, phase table, next actions, blockers, readiness booleans, and hardware-ready claim guard before live/target pass output, so operators can follow one report without treating it as physical acceptance evidence.

Latest product HUD/release-smoke checkpoint: Unity HUD now consumes `/api/field/operator-plan` for phase progress, hardware-ready claim guard, next action, and blocker display; `server:smoke` verifies packaged localhost release endpoints plus field-live-pass while preserving hardware-ready=false for fresh packages.

Latest hardware probe checkpoint: `device:probe` now sees the same Station Pro through sanitized USB and TCP ADB transports, both in `device` state. `station:glasses:diagnose` selects USB and still reports internal display only, `dsp_connected=false`, USB role device/MTP, head-pose failures, and `hardware_ready_claim_allowed=false`.

## 2026-07-04

- Productization checkpoint at 21:59 Asia/Shanghai: added read-only `/api/field/operator-plan` and a Web Field Pass panel as the鐜板満 P0 command center. The plan exposes six phases only: preflight, A1 spatial entry, A2 memory read, A3 TimeMark write-back, User B readback, and closeout.
- The operator plan is now on the shared endpoint bus and Unity/Web contracts: `shared/innerworld-contract.js`, `api-router`, Unity `SpaceEndpointMap`, Unity `SpaceApiClient`, `check-contract`, and `check-web-demo` all know `/api/field/operator-plan`. The Web panel uses the API when available and falls back to local derivation from `/api/field/acceptance` and `/api/field/target-readiness`.
- Runtime HTTP smoke on a temporary current-code server at `127.0.0.1:5198` returned `schema=innerworld-field-operator-plan/v1`, `current_phase=preflight`, `total_phases=6`, `precheck_ok=true`, `physical_acceptance_ready=false`, `hardware_ready_claim_allowed=false`, `write_back_beacon_count=0`, and privacy guards false for raw identifiers/logs/dumps. Bootstrap also exposed `field_operator_plan=/api/field/operator-plan`.
- Verification for this productization slice: JS syntax checks, `check:web`, `check:unity`, `check:field-acceptance`, `check:readonly`, `check:contract`, `check:mainline`, and scoped `git diff --check` passed. Playwright/browser visual automation was not available in this workspace; HTTP/static/runtime contract checks were used instead.
- Carver completed the `f440a388` checkpoint review in `.agents/carver/carver-f440a388-adb-display-diagnostics-audit-2026-07-04.md`: no hardware-ready overclaim, display/glasses gate remains expected-red, and the next product direction should be operator flow, local/LAN run package, release gates, and Unity/Web P0 status experience without guide/PPT/phone-only/UGC/backend/route expansion.
- C drive guardrail: ran `cache:temp:report` then `cache:temp:clean`; only script-approved, unreferenced Visual Studio installer temp candidates were deleted. Free space improved from about 10.65GB to about 18.19GB, and project/Unity/Android build caches were left intact.
- Station black-screen follow-up at 21:33 Asia/Shanghai: the APK/OpenXR loader path remains fixed, so the current user-visible black screen is tracked as a physical Station Pro -> glasses display-chain blocker. The scripts now tolerate USB+TCP ADB duplicate transports by selecting one sanitized target, preferring USB, and recording only the selected transport/hash prefix.
- Current read-only evidence sharpens the blocker: Android display enumeration remains internal-only; `rokid_display` reports USB display/device presence but `dsp_connected=false`; Station USB state shows device/MTP mode toward Windows, so the new blockers are `rokid_display_dsp_not_connected` and `station_usb_role_device_mode_blocks_glasses`. This is not hardware-ready and does not unblock A1/A2/A3 physical acceptance.
- The strict commands stay intentionally red until the physical display session is fixed: `station:glasses:require-ready` blocks on the display/DSP/USB-role lane, and `station:apk:display-smoke` still proves install/cold-launch/process while failing on `rokid_glasses_display_not_detected`.
- Read-only glasses diagnostics checkpoint at 20:47 Asia/Shanghai: added `tools/station-pro-glasses-diagnostics.ps1` with npm commands `station:glasses:diagnose` and `station:glasses:require-ready`. The tool does not install/launch the APK and writes only sanitized summaries/counts/hashes for ADB, display, Rokid/OpenXR packages, runtime service, input, USB, selected properties, and runtime log patterns.
- Current diagnostic result: `station:glasses:diagnose` exits green as a report with `openxr_runtime_package_ready=true`, `runtime_ready=true`, `input.external_input_candidate_count=9`, and USB connected/accessory tokens present. It still reports `external_display_detected=false` and `glasses_display_ready=false`. `station:glasses:require-ready` is expected-red with `rokid_external_display_not_detected` and `rokid_head_pose_failure_detected`.
- Boundary: this confirms the next blocker is the physical Station Pro -> Rokid Max/HDMI/glasses display/head-pose chain. It does not create trusted A1/A2/A3 observations and does not allow a hardware-ready claim.
- Display/glasses gate checkpoint at 20:30 Asia/Shanghai: `tools/station-pro-apk-smoke.ps1` now has a sanitized display/runtime diagnostic path. Mutating APK smoke clears logcat before launch, then records display summaries from `dumpsys display`, Rokid runtime log pattern counts, `glasses_display_ready`, and blocker ids without writing raw dumpsys or raw logcat. New strict script: `npm run station:apk:display-smoke`.
- Current fresh Station Pro evidence: `station:apk:smoke` passes for APK `5b1d4641616d...` with install OK, cold launch OK, process observed, `is_uxr_app=true`, `rokid_runtime_loaded=true`, `runtime_unavailable_count=0`, and `runtime_broker_failure_count=0`. It warns `rokid_external_display_not_detected` and `rokid_glass_name_not_detected`; display evidence is one internal display, no external display.
- Expected-red strict display gate: `npm run station:apk:display-smoke` fails with `rokid_glasses_display_not_detected` while still proving install/run smoke. This is now the official local blocker for the remaining black-screen risk before any physical A1/A2/A3 scan.
- Black-screen loader fix checkpoint at 20:11 Asia/Shanghai: the reported Station Pro black-screen cause was traced to the final APK packaging Unity's default `libopenxr_loader.so`, which searched for the Khronos runtime broker instead of Rokid's runtime package. The build wrapper now replaces generated `unityLibrary/libs/openxr_loader.aar` with the Rokid package AAR, removes the generated project default naked loader before repack, and requires the APK loader to contain `com.rokid.openxr.runtime`.
- Current APK after the fix: `output/unity-android/InnerWorldRokid.apk`, 45,798,669 bytes, SHA256 `5b1d4641616dfc75bee98f1772af5c592820b07ba90c0e38413dff2bdd253029`. APK evidence shows `lib/arm64-v8a/libopenxr_loader.so`, size 1,616,288, SHA prefix `b7e723e8017e`, `contains_rokid_runtime_package_marker=true`, plus `librokid_openxr_api.so` and `libyuv.so`.
- Station Pro validation for this checkpoint: `station:apk:smoke` passed with install OK, cold launch OK, process observed, and `is_uxr_app=true`. Fresh logcat summary at `output/black-screen-diagnostics/20260704-200626/summary.json` shows `fatal=0`, `dll_not_found=0`, `unsatisfied=0`, `xr_runtime_unavailable=0`, `runtime_broker_failed=0`, and Rokid runtime manifest/load success.
- Verification run: PowerShell parse for `tools/build-unity-android.ps1` and `tools/station-pro-apk-smoke.ps1`, `node --check server/space-server/check-station-pro-apk-smoke.js`, `station:apk:inspect`, `station:apk:smoke`, `check:station-apk:rkimage`, `check:uxr-readiness:ready`, `check:unity`, `check:contract`, and `check:mainline` passed. `field:target-pass:strict` remains expected-red on missing live operator-paired SDK session/current target diagnostics preflight/trusted A1-A3/P0 mission loop, so hardware-ready remains false.
- Residual black-screen/display lane: the previous OpenXR runtime-unavailable blocker is fixed, but `dumpsys display` still shows only the Station Pro internal display and runtime logs include `getGlassName failed: glass not detected` plus head-pose failures. Next work should add a display/glasses detection gate and investigate physical Max/HDMI/glasses detection before any visual hardware-ready claim.
- Target-pass provenance input checkpoint at 18:30 Asia/Shanghai: `field:target-pass --apply-mission-actions` now attaches current online operator-paired live session/device/anchor inputs to A2 read/find_year, controlled service action, A3 TimeMark write-back, and User B readback requests. The report exposes only sanitized `provenance_input` summaries (`session_available`, hash prefixes, anchor, input-confirm status, and raw-id guards), letting the server evaluate trusted mission provenance without publishing raw session ids or raw device ids.
- Target-pass mutation guard hardening at 18:35 Asia/Shanghai: target-pass mutation now refuses to write mission state when the current live provenance input is incomplete. The required pre-write signal is active anchor matching the action anchor, RKInput ray reported, PointableUI focus, and confirm input; missing pieces are surfaced as sanitized `provenance_blockers` such as `active_anchor_not_target`, `pointable_ui_focus_missing`, or `input_confirm_missing`.
- Verification for this checkpoint: `node --check tools/field-target-pass.js`, `check:contract`, `check:field-target-pass`, `check:mainline`, and expected-red `field:target-pass:strict`. Current runtime still lacks trusted A1/A2/A3, so action mutation remains skipped on `trusted_A2_missing` / `trusted_A3_or_service_action_missing`; hardware-ready remains false.
- Field report operator clarity checkpoint at 18:22 Asia/Shanghai: `field-live-pass` and `field-target-pass` Markdown now show `Mission ledger ready`, `Mission trusted A1/A2/A3 prerequisite ready`, `Mission missing trusted anchors`, and `Trusted mission provenance ready` in the top summary. `field-live-pass` next actions now explicitly say not to treat a completed mission ledger/User B/provenance chain as physical acceptance until trusted anchors are rescanned. `docs/field-hardware-runbook.md` fail-fast rules include `mission_loop_waiting_for_trusted_a1_a2_a3`.
- Verification for this checkpoint: `check:contract`, `field:live-pass -- --single`, `check:field-target-pass`, expected-red `field:target-pass:strict`, `check:field-acceptance`, `check:field-acceptance -- --api`, `check:mainline`, and scoped `git diff --check`. Current runtime was reset to rehearsal state by `check:unity`, so `mission_ledger_ready=false` in the latest live reports is expected; hardware-ready remains false.
- Mission-loop physical prerequisite checkpoint at 18:15 Asia/Shanghai: `/api/field/acceptance`, `/api/field/target-readiness`, `field-live-pass`, and `field-target-pass` now keep `mission_loop_ready=false` when the mission ledger/User B/provenance side is complete but trusted A1/A2/A3 observations are still missing. Reports expose `mission_ledger_ready=true`, `trusted_a1_a2_a3_ready=false`, `missing_trusted_anchor_ids=[A1,A2,A3]`, and the strict blocker `mission_loop_waiting_for_trusted_a1_a2_a3`.
- Verification for this checkpoint: `check:field-acceptance`, `check:field-acceptance -- --api`, `field:live-pass -- --single`, `check:field-target-pass`, expected-red `field:target-pass:strict`, `check:device`, `check:contract`, and `check:mainline`. Hardware-ready remains false; this change prevents a green mission ledger from being mistaken for physical acceptance.
- Current-APK Station Pro rebuild/smoke at 17:55 Asia/Shanghai: GitHub branch `codex/rokid-real-device-sync` was pushed first and PR #1 was clean/CI-green before continuing. The LAN Space Server was restarted on current code at `0.0.0.0:5177`. A Unity compile blocker in `InnerWorldDemoController.BuildDeviceInputFramePayload` was fixed, then the current APK was rebuilt from current Unity code: `output/unity-android/InnerWorldRokid.apk`, 45,727,279 bytes, SHA256 `19733d32b2bdbd347895a319e55a051c7c3722d1329f823baf78ce61e9978955`.
- `station:apk:pair-smoke` passed on the connected Station Pro for the `19733d32b2bd...` APK: install OK, cold launch OK, process observed, `is_uxr_app=true`, operator pairing issued/injected/verified, and evidence kept raw serials, session ids, pairing codes, MACs, and private IPs out. `check:uxr-readiness:ready` now has no current-vs-mutating-launch SHA mismatch and still says `hardware_ready_claim_allowed=false`.
- Runtime gates after the smoke: `check:station-apk:rkimage`, `check:uxr-readiness:ready`, `check:device`, `field:live-pass -- --single`, and `check:field-target-pass` passed as package/live-window checks. `field:target-pass:strict` remains expected-red with `trusted_a1_a2_a3_observations_missing`; physical acceptance is false until fresh trusted A1/A2/A3 observations plus the A3 TimeMark/User B loop pass on the real wall.
- Build wrapper hardening follow-up: the first rebuild exposed that `tools/build-unity-android.ps1 -RunPostChecks` could still hang in its PowerShell Job wrapper after Unity success. `Invoke-ExternalForReport` now uses a direct .NET `ProcessStartInfo` runner with async stdout/stderr capture and timeout, and `powershell -File tools/build-unity-android.ps1 -SkipUnityBuild -RunPostChecks -RequirePostCheckDevice` passes for the current APK.
- Trusted mission provenance checkpoint at 17:28 Asia/Shanghai: Carver's short audit is recorded in `.agents/carver/carver-trusted-mission-provenance-audit-2026-07-04-1728.md`. Added `trusted_mission_provenance_gate/v1`: Unity requests for interaction, service action, write-back, and User B readback carry session/device/anchor inputs; the Space Server derives sanitized proof from the current operator-paired live session, input frame focus/ray, and confirm input; SQLite summarizes trusted mission provenance; `/api/field/acceptance` requires it inside `mission_loop`.
- Boundary for this checkpoint: raw session ids, raw device ids, private network identifiers, pairing codes, raw pose streams, raw ray vectors, and camera frames are not returned in ledger/API proof. Script/manual/simulator writes can still rehearse state, but `field-live-pass` and `field-target-pass` keep `mission_loop_ready=false` and strict target pass reports `trusted_mission_provenance_missing` until trusted live Rokid provenance exists.
- Verification for this checkpoint: `check:unity`, `check:device` against temporary current-code server `127.0.0.1:5201`, `check:contract`, `check:field-acceptance`, `check:field-target-pass`, `check:mainline`, `context:export`, and scoped `git diff --check` passed. `field:target-pass:strict` is expected-red and now includes `trusted_mission_provenance_missing`.
- RKInput/PointableUI heartbeat checkpoint at 17:08 Asia/Shanghai: Unity `DeviceHeartbeatRequest` now includes sanitized `input_frame` evidence from `RokidInputFrame` for RKInput 3DoF ray and PointableUI focus over A1/A2/A3. The Space Server sanitizes and stores only the summary (`focused_anchor_id`, command/buttons, hit distance, `ray_reported`, `pointable_ui_focus`) in device health, sessions, and the live-adapter checklist; raw ray vectors are not returned. This does not claim hardware-ready and does not weaken `/api/field/acceptance`.
- Verification for this slice: `node --check server/space-server/src/domain/device-runtime.js`, `node --check server/space-server/check-contract.js`, `node --check server/space-server/check-device.js`, `npm run check:unity`, `npm run check:contract`, `npm run check:device`, `npm run check:mainline`, `npm run context:export`, and scoped `git diff --check` passed. `check:device` used a temporary current-code server on `127.0.0.1:5199`; the existing `127.0.0.1:5177` process was left untouched.
- Target readiness operator panel checkpoint at 16:52 Asia/Shanghai: Carver's direction audit is recorded in `.agents/carver/carver-target-readiness-panel-audit-2026-07-04-1640.md`. Added read-only `/api/field/target-readiness`, derived from `/api/field/acceptance`, and wired Web Field Acceptance cards for `precheck_ok`, `physical_acceptance_ready`, trusted A1/A2/A3 count, mission/User B state, and physical blockers. Web does not read `output/*.json`, run ADB/logcat, create simulator/manual observations, or mutate mission state.
- Runtime verification for this slice: `check:field-acceptance`, `check:web`, `check:contract`, and `check:mainline` passed. A localhost API smoke on `127.0.0.1:5188` returned `physical_acceptance_ready=false`, `hardware_ready_claim_allowed=false`, missing A1 raw hardware evidence, missing trusted A1/A2/A3, missing P0 mission/User B loop, and `/api/field/acceptance` still `rehearsal_ready`.
- Field target session wrapper checkpoint at 16:32 Asia/Shanghai: Carver re-audited the lane in `.agents/carver/carver-target-session-wrapper-audit-2026-07-04-1632.md` with no hardware-ready misclaim or P0 drift. `field-target-pass` now reports `precheck_ok`, `physical_acceptance_ready`, and `physical_blockers`; current precheck is true while physical acceptance remains false on missing trusted A1/A2/A3, P0 mission/User B loop, and field acceptance. `field:acceptance-session:target` wraps pair-smoke/live watch/target-pass diagnostics, and `field:acceptance-session:target-strict` remains red until real A1/A2/A3/User B evidence exists.
- Verification for the wrapper slice: `check:contract`, `check:mainline`, `check:field-target-pass`, `field:target-pass:apply`, `field:acceptance-session:target -- -WatchDurationSec 3 -WatchIntervalSec 1`, and expected-failing `field:acceptance-session:target-strict -- -WatchDurationSec 3 -WatchIntervalSec 1`. The strict run left `hardware_ready_claim_allowed=false`, `mission_or_writeback_mutated=false`, and `/api/state` at User A / `entered` / no completed steps.
- Station Pro current-APK smoke at 16:17 Asia/Shanghai: GitHub branch `codex/rokid-real-device-sync` and PR #1 were synced with CI green before this hardware step. The rebuilt APK `output/unity-android/InnerWorldRokid.apk` is 45,722,295 bytes with SHA256 `9ddf80932c9896c3c744f6a46bef104e6722bd0615675f1a83e364db2adafe4e`, and `station:apk:pair-smoke` passed on the connected Station Pro for that exact SHA: install OK, cold launch OK, process observed, `is_uxr_app=true`, operator pairing issued/injected/verified, and no raw pairing code/session/device identifiers persisted in evidence.
- Post-smoke gates passed: `check:station-apk:rkimage`, `check:uxr-readiness:ready`, `check:field-live-pass`, `check:field-target-pass`, `check:unity`, `check:contract`, `check:mainline`, and `context:export`. `field:target-pass:strict` still fails correctly with `trusted_a1_a2_a3_observations_missing` and `p0_mission_writeback_user_b_loop_missing`; hardware-ready remains false until fresh physical A1/A2/A3 scans, A2 read, controlled service action, A3 TimeMark write-back, User B readback, and `/api/field/acceptance` pass.
- Trusted-observation rescan barrier checkpoint at 16:01 Asia/Shanghai: Carver wrote `.agents/carver/carver-trusted-observation-checkpoint-2026-07-04.md` and cleared the next slice with no hardware-ready claim. Unity now queues one latest pending A1/A2/A3 target event per anchor when a target is seen before the server acknowledges the same session as operator-paired, hardware-eligible, and input/overlay/live SDK-bound; after heartbeat ack, the queued observation is retried through the normal trusted target POST path.
- Field live/target reports now expose `Live Adapter Binding`: latest operator-paired session is `live_binding_ready`, input/overlay/live binding are true, and adapter checklist gaps are `none`. This clarifies that current A2/A3 blockers are stale/untrusted observations, not current APK/package readiness. Strict acceptance remains red until fresh trusted A1/A2/A3, A3 TimeMark write-back, and User B readback are completed on the real wall.
- Per-anchor trust diagnostics checkpoint at 15:43 Asia/Shanghai: `field-live-pass` and `field-target-pass` now include sanitized `trust_issues_by_anchor` from `/api/calibration/wall` and render an `Untrusted Hardware Observations` section in JSON/Markdown reports. The current baseline explicitly shows why existing hardware-mode observations cannot count yet: A2/A3 require re-scan/re-bind through the operator-paired live SDK session instead of being silently summarized as generic untrusted anchors.
- The new diagnostics keep the privacy boundary: reports include issue codes, tracking mode, SDK binding stage, and pairing/session status snapshots, but no raw session ids, raw device ids, pairing codes, private IPs, MAC addresses, or raw logcat. Hardware-ready remains false; strict field target/live acceptance still blocks on trusted A1/A2/A3 observations plus the A3 write-back/User B readback loop.
- APK target index map preflight at 15:25 Asia/Shanghai: `tools/station-pro-apk-smoke.ps1` and `tools/uxr-readiness.js` now open the APK-packaged `assets/RKImage.db`, parse nested `Data.json`, and require `innerworld-rokid-target-index-map/v1` with `1:A1`, `2:A2`, `3:A3`. Current APK `e447...` is green: `check:station-apk:rkimage` reports `rkimage_db_target_index_map_ready=true`, `check:uxr-readiness:ready` shows no blockers, and `field:target-pass` now surfaces `target_index_map_ready=true` in its target diagnostics preflight. This still only proves package/target-map freshness; physical A1/A2/A3 observations and User B readback remain missing.
- Current target-diagnostics APK checkpoint at 14:31 Asia/Shanghai: rebuilt `output/unity-android/InnerWorldRokid.apk` from current Unity code. New APK is 45,721,247 bytes with SHA256 `e447069ac12b8b757e143387975a06a6965e0ede50b91e77d78763b5adf39c84`; it is LAN-ready, UXR-manifest-ready, packages `assets/RKImage.db` plus `libopenxr_loader.so`, `librokid_openxr_api.so`, and `libyuv.so`, and contains `IW_TARGET_EVENT`, `IW_TARGET_POST_RESULT`, and `IW_TARGET_MISSION_ASSIST` in APK metadata.
- Installed and cold-launched the new APK on Station Pro with operator pairing. `station:apk:pair-smoke`, `check:station-apk:rkimage`, `check:uxr-readiness:ready`, `uxr:doctor`, `check:contract`, `check:mainline`, and `check:field-live-pass` passed. Latest live-pass remains correctly non-ready for hardware: trusted A1/A2/A3, mission loop, User B readback, and field acceptance are still false.
- `field:target-pass` now records `target_diagnostics_preflight`: current APK SHA prefix, APK `IW_TARGET_*` token scan, latest mutating Station Pro launch evidence, and UXR readiness must agree. `check:field-target-pass` shows this preflight ready for `e447...`; `field:target-pass:strict` now requires it and still fails only on missing trusted A1/A2/A3 plus P0 mission/User B loop.
- `field:target-pass:watch` is now the read-only鐜板満 scan watcher: it samples repeated phase/API snapshots and `IW_TARGET_*` logcat counts without writing raw logcat. A short `--duration-sec 3 --interval-sec 1 --logcat` smoke produced 3 snapshots, preflight ready, and zero target counts before physical scanning.
- Fixed the Unity build wrapper after the real build exposed a post-Unity hang: `tools/build-unity-android.ps1` now runs external report commands through a timeout-bounded job wrapper and reports `timed_out` / `timeout_seconds`. `-SkipUnityBuild` no longer rebuilds `RKImage.db`, so report refresh stays non-mutating. Verified with `powershell -File tools/build-unity-android.ps1 -SkipUnityBuild -RunPostChecks -RequirePostCheckDevice`, which now writes `unity-build-android-latest.*` for the current APK.
- Target observation diagnostics checkpoint at 14:10 Asia/Shanghai: Unity now emits stable `IW_TARGET_*` log tokens for Rokid image target events, unknown image indexes, live-pairing/session gates, throttling, POST start/result/failure, and trusted mission-assist outcomes. `field:live-pass:watch --logcat` counts these tokens without writing raw logcat, pairing codes, session ids, serials, private IPs, or MAC addresses.
- Verified after the diagnostics change: `git diff --check`, `npm run check:unity`, `npm run check:contract`, `npm run check:field-live-pass`, `npm run check:field-target-pass`, `npm run check:mainline`, and `npm run context:export`. Latest live-pass still shows one operator-paired live session but trusted A1/A2/A3, mission loop, User B readback, and field acceptance remain false.
- Field acceptance session runner added and validated at 13:06 Asia/Shanghai: `tools/field-acceptance-session.ps1` and npm scripts `field:acceptance-session`, `field:acceptance-session:live`, and `field:acceptance-session:strict` now bundle real-device prechecks, endpoint snapshots, Station Pro pair-smoke when explicitly requested, and live-pass watching into one sanitized field evidence report. Default mode is non-mutating and does not post simulator/manual observations or mission/write-back state.
- Latest `npm run field:acceptance-session` pass is green for the precheck wrapper: strict device probe OK, APK inspect OK, one operator-paired live session ready, and `hardware_ready_claim_allowed=false`. The next physical actions remain A1 scan, A2/A3 re-bind through the operator-paired live SDK session, A2 read, A3 write-back, and User B readback.
- Strict live-pass still blocks as expected with `trusted_a1_a2_a3_observations_missing` and `p0_mission_writeback_user_b_loop_missing`; this confirms the new runner did not weaken the hardware-ready boundary.
- Carver short diff audit found the runner direction/privacy OK and flagged strict exit-code semantics. The runner now propagates `field-live-pass` JSON `ok=false` into the session command/result, so `-RequireTrusted -RequireMissionLoop` exits non-zero until real physical evidence exists.
- Field live-pass watch checkpoint at 12:41 Asia/Shanghai: `tools/field-live-pass.js` now reports `hardware_a1_a2_a3_ready`, `missing_trusted_anchor_ids`, `missing_hardware_anchor_ids`, `missing_mission_step_ids`, and `next_required_actions`. This keeps the tool read-only while giving the field operator exact next steps during the physical A1/A2/A3/User B pass.
- Latest live-pass baseline is more specific: one operator-paired live session is online, trusted anchors are still `0/3`, raw hardware alignment is missing `A1`, and A2/A3 currently need re-scan or re-bind through the operator-paired live SDK session because their existing hardware-mode observations are not trusted. Hardware-ready remains false.
- Real-device probe hardening checkpoint at 12:31 Asia/Shanghai: `tools/device-probe.ps1` now wraps ADB/tool version calls and Windows PnP enumeration with bounded timeouts, records `timeouts.command_seconds`, `timeouts.pnp_seconds`, `adb_timed_out`, and `pnp_timed_out`, and keeps the same sanitized privacy boundary. `server/space-server/check-device-probe.js` now has an outer 90s PowerShell timeout and asserts the timeout schema so `npm run check:device-probe` cannot hang indefinitely.
- Carver completed a fresh source/PDF audit for this checkpoint at `.agents/carver/carver-source-audit-2026-07-04-1231.md`. It confirms the group/teammate materials still converge on the P0 one-wall Rokid loop, and that 19 target PDFs were readable through PyMuPDF text extraction. The audit repeats that hardware-ready remains unproven until the real A1/A2/A3/User B loop passes under trusted live SDK evidence.
- Carver diff audit for this checkpoint is `.agents/carver/carver-diff-audit-2026-07-04-1234.md`; recommendation is `push`, with no P0 drift, no privacy-boundary expansion, and no evidence-pointer blocker found.
- Latest strict device probe is green: one sanitized Station Pro-class USB ADB device is in `device` state, Android SDK/Unity/Java/Maven/Node are visible, PnP summary has 12 sanitized devices, and there are no warnings. This is still connected-device evidence, not hardware acceptance.
- Station APK evidence split fix: after a mutating `station:apk:pair-smoke`, `check:station-apk:rkimage` now reads `station-pro-apk-smoke-latest-inspect.*` instead of the generic latest pointer, and asserts `evidence_kind=inspect_only`. This prevents non-mutating package gates from accidentally consuming install/launch evidence.
- LAN Space Server was restarted on `0.0.0.0:5177`; `npm run station:apk:pair-smoke` passed again for current APK SHA `bd852f7012e25f9ccd2630e2113a1a3526fc7bdfea5d05c32d56c410303fe142`; `npm run check:field-live-pass` is green for one online operator-paired live session while trusted A1/A2/A3, mission loop, User B readback, and field acceptance remain false. The strict command `node tools/field-live-pass.js --single --require-trusted --require-mission-loop` still blocks as expected.
- Verified after this hardening: `npm run check:device-probe`, `npm run device:probe:strict`, `npm run station:apk:inspect`, `npm run check:station-apk:rkimage`, `npm run check:mainline`, `npm run check:contract`, and `npm run check:field-live-pass`.
- PR #1 follow-up checkpoint: Carver reviewed commit `1358664f` as directionally safe for the P0 real-device lane, with the next work locked to physical A1/A2/A3 observations, A3 TimeMark write-back, User B readback, and field acceptance.
- Field acceptance now requires explicit User B readback. The `mission_loop` gate requires `user_b_readback`, exposes `active_user`, `required_active_user=B`, and `user_b_readback_ready`; completed mission steps plus an A3 write-back beacon are not enough unless `/api/state.active_user` is `B`.
- `tools/field-live-pass.js` now includes `user_b_readback_ready` in `mission_loop_ready`; the strict negative command `node tools/field-live-pass.js --single --require-trusted --require-mission-loop` blocks as expected with `trusted_a1_a2_a3_observations_missing` and `p0_mission_writeback_user_b_loop_missing`.
- LAN Space Server was restarted onto the current code, `npm run station:apk:pair-smoke` passed again on the connected Station Pro for current APK SHA `bd852f7012e25f9ccd2630e2113a1a3526fc7bdfea5d05c32d56c410303fe142`, and `npm run check:field-live-pass` is green for the live operator-paired session window while still showing trusted A1/A2/A3, User B readback, and field acceptance as false.
- Verified after the change: `npm run check:field-acceptance`, `npm run check:field-acceptance -- --api`, `npm run check:unity`, `npm run check:contract`, `npm run check:field-live-pass`, and the expected-blocker strict field live pass command.

## 2026-07-03

- Station Pro native-lib checkpoint passed at 23:14-23:17 Asia/Shanghai. Current APK: `output/unity-android/InnerWorldRokid.apk`, 45,719,155 bytes, SHA256 `bd852f7012e25f9ccd2630e2113a1a3526fc7bdfea5d05c32d56c410303fe142`.
- `tools/build-unity-android.ps1` now treats missing Rokid native libraries as a build failure. If Unity batchmode creates an APK that lacks `librokid_openxr_api.so` / `libyuv.so`, it automatically reassembles the Unity-generated Gradle project after staging those libraries from the Rokid OpenXR AAR into generated `jniLibs`.
- APK inspect and release gate now require both `assets/RKImage.db` and Rokid/OpenXR native libraries. `npm run station:apk:inspect` and `npm run check:station-apk -- --skip-smoke --require-lan --require-rkimage-db --require-rokid-native-libs` passed with `libopenxr_loader.so`, `librokid_openxr_api.so`, and `libyuv.so` packaged for `arm64-v8a`.
- `npm run station:apk:pair-smoke` passed twice on the connected Station Pro for the current APK: install OK, cold launch OK, process observed, `is_uxr_app=true`, operator pairing issue/injection/verification OK, and no raw pairing code persisted in evidence.
- After clearing all logcat buffers and relaunching the current APK, sanitized pattern counts were `DllNotFoundException=0`, `rokid_openxr_api=0`, `UnsatisfiedLinkError=0`; the previous `rokid_openxr_api` blocker is resolved for this build.
- `npm run check:uxr-readiness:ready` passed. Runtime field acceptance is still correctly `rehearsal_ready`, not hardware-ready: trusted hardware observations remain `0/3`, hardware alignment still lacks trusted A1/A2/A3 evidence, and the P0 mission/write-back loop has not been completed on the live device.
- Unity image tracking path is ready for the next physical wall pass: Rokid image index `1/2/3` maps to `A1/A2/A3`; A1 posts `qr`, A2/A3 post `image_tracking`; posts are gated by an operator-paired live SDK session before they can count as trusted hardware evidence.
- Added read-only鐜板満 live monitor `tools/field-live-pass.js` with npm scripts `field:live-pass`, `field:live-pass:watch`, and `check:field-live-pass`. Current snapshot proves the live operator-paired session window is open (`live_session_ready=true`, one online live operator-paired session) while correctly showing `trusted_a1_a2_a3_ready=false`, `mission_loop_ready=false`, and `field_acceptance_ready=false`.
- Negative acceptance command is wired: `npm run field:live-pass -- --single --require-trusted` fails with `trusted_a1_a2_a3_observations_missing`, without raw session ids or raw logcat. Use this during the physical target pass to avoid accidentally counting rehearsal evidence.
- C drive was checked and is about 17.5GB free. No broad cleanup was performed; future cleanup must stay inside project-generated Rokid outputs/caches or guarded scripts only.

- Evidence-chain reconciliation completed at 22:18 Asia/Shanghai. The current disk APK is `output/unity-android/InnerWorldRokid.apk` at 45,167,409 bytes with SHA256 `ce3f118632d6202c61455f19cdc11080a765e17c74f6be07496ef2c2b571cf1d`.
- `tools/build-unity-android.ps1` now records final APK size/SHA after post-checks. A non-rebuild refresh command, `powershell -File tools/build-unity-android.ps1 -SkipUnityBuild -RunPostChecks -RequirePostCheckDevice`, updated `unity-build-android-latest.*` as `existing_apk_only`; it did not install or launch the APK.
- Current non-mutating evidence is green: `npm run check:station-apk:lan`, `npm run uxr:doctor`, and `npm run check:uxr-readiness:ready` passed. The current APK inspect evidence matches SHA `ce3f118632d6...`, is `private_lan`, has `network_ready_for_device=true`, and keeps UXR manifest markers.
- Boundary correction: the last mutating launch evidence is still valid only for previous APK SHA `70592a5dfec4...`, not for the current `ce3f118632d6...` APK. `uxr-readiness-latest` now warns `latest_mutating_launch_apk_sha_mismatch_current_apk` and sets the next required proof to `user_confirmed_current_apk_station_pro_install_launch_smoke`.
- Hardware-ready remains false. No new current-APK install/launch, operator-paired live SDK binding, trusted A1/A2/A3 observations, A3 write-back, User B readback, or `/api/field/acceptance` pass was performed in this reconciliation checkpoint.

- Station Pro UXR manifest/install/run checkpoint passed at 21:05 Asia/Shanghai. The rebuilt APK is `output/unity-android/InnerWorldRokid.apk` (24,753,149 bytes, SHA256 `f4c1a909cbcd11af98d1ca9dd53a2ca7ada1248a60c9c78e12b718786ca19884`). It embeds LAN config, `com.rokid.sdk=uxr`, `com.rokid.uxr.application.mode=3d`, OpenXR/UXR package queries, camera/audio permissions, and `uxr_manifest_ready=true`.
- `npm run station:apk:smoke` now installs and launches on the connected Station Pro: install OK, `am start` status OK, process observed, `is_uxr_app=true`, and no launch error. This supersedes the earlier error-102 blocker for the plain Android package.
- Space API live heartbeat is now proven over LAN from the running Unity app: `/api/device/sessions` shows one online `unity-shell-rokid-ar` session with heartbeat count increasing, health `ok`, active anchor `A3`, and pose present.
- Boundary remains strict: this is install/run plus LAN heartbeat, not official Rokid SDK live binding. The session still reports `sdk_binding_status.stage=fallback_only`, `live_binding_ready=false`, pairing is unpaired, and `hardware_acceptance_eligible=false`; RKCameraRig/RKInput/PointableUI/image target/SLAM trusted A1-A3 evidence remain P0.
- Fixed `tools/station-pro-apk-smoke.ps1` so launch diagnostics are queried after install, avoiding stale `isUxrApp=false` from the previously installed APK.
- Hardened smoke/readiness evidence at 21:19 Asia/Shanghai: `station-pro-apk-smoke` now writes separate latest inspect and latest mutating-launch pointers, and `check:uxr-readiness` no longer requires the superseded error-102 blocker when the latest mutating launch passed.

- Superseded 20:48 blocker audit: `.agents/carver/uxr-blocker-audit-2026-07-03.md` correctly identified Station Pro UXR launch acceptance as the blocker; the 21:05 UXR manifest smoke resolved that specific install/run blocker.
- Earlier 20:48 smoke history: the previous Gradle LAN APK installed but did not launch, with `is_uxr_app=false` and error `102`. Keep this only as regression context for why `uxr_manifest_ready` is now a hard APK check.
- Current boundary is strict: package/LAN/install/run/heartbeat evidence exists, but hardware-ready remains false. Missing proof: official UXR/OpenXR package validation, RKCameraRig/RKInput/PointableUI binding, trusted A1/A2/A3 QR/image_tracking/SLAM observations, operator pairing, and `/api/field/acceptance`.
- Next P0 action: keep the UXR manifest gate, then install `com.rokid.xr.unity`, run Environment Fix / OpenXR Feature Groups / Project Validation, and bind the live Rokid SDK adapters so heartbeat stage can move beyond `fallback_only`.
- C drive is still tight before UXR import/build work: guarded temp cleanup removed one old Visual Studio installer temp item (~0.121GB) and left C at about 23.77GB free. Preserve Unity/Android caches; keep monitoring before another large Unity/Gradle/UXR operation.

- Direct LAN Gradle APK rebuild completed at 20:15-20:18 Asia/Shanghai. The generated Unity Gradle project was patched with Aliyun Maven mirrors and `:launcher:assembleRelease` succeeded; `launcher-release.apk` was copied to `output/unity-android/InnerWorldRokid.apk` (24,752,749 bytes). This supersedes the earlier config-only patched APK as the current standard output artifact.
- Latest non-mutating APK evidence now passes as LAN-ready: `npm run station:apk:inspect` reports package `com.innerworld.rokid.prototype`, launchable `com.unity3d.player.UnityPlayerGameActivity`, INTERNET permission, cleartext/network security enabled, `config_host_kind=private_lan`, `network_ready_for_device=true`, and `space_id=innerworld_campus_wall`. `npm run check:station-apk:lan`, `npm run device:probe`, `npm run check:device-probe`, and `npm run check:mainline` also passed.
- `npm run field:preflight -- -RequireLan` passed after the rebuild and regenerated the field kit/config. The Unity source config host hash matches the embedded APK config host hash, so the field kit and APK are aligned without printing raw private IPs.
- No Station Pro install or launch was performed in this 20:18 checkpoint. The result proves LAN-ready package/config evidence only; live heartbeat, official UXR/OpenXR binding, trusted A1/A2/A3 observations, and `/api/field/acceptance` remain pending.
- User preference recorded: if Gradle/Maven dependency downloads fail again, use IDM (`IDMan.exe`) to fetch failed URLs into a controlled local Maven/Gradle cache. This pass did not require IDM because the mirror patch cleared the dependency failures. Follow-up hardening: move mirror/IDM fallback out of generated `Library/Bee/.../Gradle/settings.gradle` so a fresh Unity export does not erase it.
- C drive free space after this checkpoint is about 26GB. Continue guarded cache/temp cleanup only; keep useful Unity/Android build caches for the next hardware pass.

- Real-device LAN APK checkpoint advanced at 20:12-20:15 Asia/Shanghai. The Space Server was restarted in LAN mode on `0.0.0.0:5177`; `npm run field:preflight -- -RequireLan -SkipPdf -SkipCacheReport` passed and updated Unity `StreamingAssets` / `Resources` config to a private-LAN URL while reports/stdout redact the IP.
- Added privacy-safe LAN gates: `npm run check:station-apk:lan` now fails if the APK config is still `localhost`, and `field-preflight` / `unity:config` no longer print raw private IPs in evidence output. Latest privacy scans for field preflight and APK patch reports found no raw private IP or raw identifier keys.
- Unity Android batch rebuild reached Gradle but failed on external dependency TLS handshakes. To keep the hardware lane moving, `npm run station:apk:patch-lan` now performs a config-only APK patch: replace `assets/innerworld-config.json`, remove old signatures, zipalign, debug-sign, and verify v2 signature. This produced `output/unity-android/InnerWorldRokid.apk` with SHA256 `3c28976481e2cc742e5dca5e1ca9dbf94beab0da00aec5469e2b6499ae822101`, `config_host_kind=private_lan`, and `network_ready_for_device=true`.
- `npm run station:apk:smoke` was run against the connected Station Pro. ADB install succeeded and the launch intent was attempted, but Android refused to start the activity with error code `102`; diagnostics show `is_uxr_app=false` and `can_launch_by_default=false`. This proves install path and Station policy boundary, not install/run smoke, live heartbeat, UXR binding, or field acceptance.
- Next blocker is now concrete: the app must become a real Rokid UXR/AR Studio app through official UXR/OpenXR package validation and manifest/runtime binding. Plain Unity Android fallback APKs can be installed but cannot be launched as the Station Pro spatial runtime.

- Fresh Carver re-audit completed for the user's "read every teammate upload again" request. Carver's write set is `.agents/carver/`; the final report is `.agents/carver/source-audit-2026-07-03.md`. It rendered `docs/绛栧垝妗?pdf` into 13 page PNGs under `.agents/carver/pdf-render/`, smoke-rendered 19 raw PDFs under `.agents/carver/pdf-render-smoke/`, and confirmed the root proposal PDF is readable with `pypdf`/`pdfplumber`.
- Carver found that old `analysis/extracted_attachments/**/pdfinfo.txt` files are mostly broken Poppler-wrapper output, not trustworthy page metadata. Future PDF metadata should use `pages.json` or the real Poppler executables under `C:\Users\33516\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\poppler\Library\bin`.
- Root attachment extraction tooling was fixed: `tools/extract_attachments.py` now prefers the real bundled Poppler executables and writes `pdfinfo.txt` only when `pdfinfo` succeeds. Verified against `docs/绛栧垝妗?pdf`: page count is 13 through the corrected path.
- Real-device evidence was rerun at 19:41 Asia/Shanghai. `npm run device:probe`, `npm run station:apk:inspect`, `npm run check:device-probe`, `npm run check:station-apk`, and `npm run check:mainline` passed. Latest probe evidence still shows one sanitized `RG_stationPro` / `stationPro` USB ADB device in `device` state, ADB 36.0.0, Android SDK build-tools/platforms, Unity Editor `6000.3.19f1`, Java/Maven/Node/npm, and no warnings from `check:device-probe`.
- GitHub was fetched and checked with `gh`: local `main`, `origin/main`, and `origin/HEAD` remain at `b21fa314`; `gh pr list` and `gh issue list` returned empty arrays for `3351666087/rokid-innerworld`. The only teammate GitHub commit found on main remains Shiyao Zhang's `f402f82f61d62e897d7615fa3f4259423e5cfce9`.
- Carver's P0 task queue is now: confirm Rokid/Unity/UXR access and hardware popups, keep sanitized probes running, replace APK `localhost` with LAN config, rebuild Android/Rokid APK, run Station Pro install/launch smoke, install official UXR, bind RKCameraRig/RKInput/PointableUI, import A1/A2/A3 target library, then prove operator-paired heartbeat and trusted `/api/field/acceptance`.
- C drive free space was checked, then `npm run cache:temp:clean` removed about 1.39GB of old Visual Studio installer temp files. C now has about 30.3GB free. One larger temp candidate was skipped because Windows reported a file lock; Unity and Android build caches were kept.
- Carver completed the full source review requested for the real-device phase. The durable local record is `docs/carver-source-review.md`: group PDFs are unpacked/readable through `text.txt` and page artifacts, P0/P1/P2 boundaries are restated, and the next work is explicitly narrowed to `station_pro_trusted_hardware_session`.
- Non-mutating Station Pro APK inspection now passes after the config parsing fix. `npm run station:apk:inspect` and `npm run check:station-apk` read the APK package/activity/manifest/config and correctly report `config_host_kind=localhost`, `network_ready_for_device=false`, one connected Station Pro-class ADB device, and `install_and_launch=false`. This is package evidence, not live heartbeat or hardware acceptance.
- Carver is the active long-line mainline reviewer sub-agent name for future checkpoint reviews. Historical notes may still mention Kepler, but new status and bus entries should use Carver.
- Local/GitHub alignment checked after `git fetch --prune origin`: local `main`, `origin/main`, and `origin/HEAD` are all at `b21fa314` (`Adopt teammate docs bus for hardware phase`). The teammate GitHub commit is `f402f82f61d62e897d7615fa3f4259423e5cfce9` by Shiyao Zhang and only adds the design/SDK/PDF reference files now governed by `docs/teammate-docs-bus.md`.
- Group-chat final-plan files were re-read from `analysis/extracted_attachments`: the 147 decision table, 170 latest group integration, 193/249 proposal lineage, and 312 hybrid Spatial Pin path all support the same P0 lock: a controlled campus memory wall / small-space hidden layer with read -> task/service action -> A3 write-back -> User B readback. Broad campus routing, open social UGC, dashboards, and platform scope stay out of the hardware lane.
- Strict local hardware probe passed at `2026-07-03T19:12:30+08:00`: sanitized ADB evidence sees one connected Station Pro-class USB device in `device` state with model `RG_stationPro` / device `stationPro`; ADB 36.0.0 is available under `C:\Program Files (x86)\Android\android-sdk\platform-tools\adb.exe`; Android SDK build-tools `36.0.0`, platforms `android-35`/`android-36`, Node, npm, Java, Maven, Unity Hub, and Unity Editor `6000.3.19f1` are present.
- Environment gaps remain explicit: `adb` is not currently on PATH, Unity is not on PATH, `ANDROID_HOME`/`ANDROID_SDK_ROOT`/`JAVA_HOME` are unset in the current shell, AR Studio / official Rokid UXR package access is not yet verified, and no live Unity/UXR APK heartbeat or trusted A1/A2/A3 hardware observation has passed. This is connected hardware, not hardware-ready acceptance.
- C drive free space was checked at roughly 23.5GB. Continue using guarded temp/cache reports and keep Unity/Android build caches only when they are useful for the next real-device pass.
- Carver final-reviewed the teammate-docs bus checkpoint and cleared it for commit/push. The remaining P0 instruction is to keep driving `station_pro_trusted_hardware_session`: UXR3.0 validation, APK install/run, live heartbeat, then operator-paired trusted A1/A2/A3 observations and `/api/field/acceptance`.
- Carver re-reviewed teammate commit `f402f82f61d62e897d7615fa3f4259423e5cfce9` for real-device adoption. The result is now recorded in `docs/teammate-docs-bus.md`: P0 merges UXR3.0 validation, RKCameraRig, RKInput 3DoF ray, PointableUI, A2/A3 image target library, SLAM/head tracking heartbeat, operator-paired live SDK proof, and trusted calibration observations; P1 keeps near/far layout, bounded gestures, controlled audio, and controlled TimeMark authoring; P2/reference keeps institution backend, social/platform features, broad routes, spatial drawing, and dashboards out of the current hardware lane.
- The 13-page teammate PDF is treated as evidence for Campus Hidden Layer / Spatial URL / fixed-place TimeMark / AI semantic compression / one-place demo, not as permission to pivot into account/social/platform scope during P0.
- Real-device phase started. The Windows development machine now sees a connected Rokid Station Pro over USB ADB as model `RG-stationPro` / device `stationPro`, Android 12, with an ADB interface and `device` state.
- Full serial numbers, USB instance ids, MAC addresses, private IPs, and pairing codes are treated as private hardware identifiers. They must not be committed to docs, release evidence, public JSON, screenshots, or GitHub.
- `station_pro_trusted_hardware_session` is the new P0 checkpoint. The immediate path is: sanitized device/toolchain probe -> operator pairing -> install/run Unity or AR Studio APK on Station Pro -> live heartbeat -> trusted A1/A2/A3 observations -> field acceptance and evidence replay.
- A second connected glasses line identified as Rokid x Bolon remains a secondary research lane. It does not replace the Max Pro + Station Pro AR Studio mainline unless it proves the same UXR/RKCameraRig/RKInput/image target/SLAM development path.
- Current evidence is enough to begin true hardware integration work, but it is not yet hardware-ready acceptance. Simulator/manual/fallback evidence still remains rehearsal only.

## 2026-07-02

- 闀挎湡鐩爣淇濇寔 active锛氬湪 `C:\Users\33516\Documents\Rokid` 鍐呯敤鏈満瀹屾暣鏉冮檺鎸佺画鎺ㄨ繘 Rokid銆岄暅瑙?InnerWorld / 鏍″洯璁板繂灞曞銆嶃€?- Windows 寮€鍙戠幆澧冨彲鐢細Unity Hub銆乁nity Editor `6000.3.19f1`銆丄ndroid SDK/NDK/OpenJDK銆丯ode銆丮aven `3.9.16` 宸插畨瑁呫€?- 鏈湴鏈嶅姟缁熶竴涓?`http://localhost:5177`锛汱AN 妯″紡鐢?`npm run dev:lan` 缁戝畾 `0.0.0.0`锛屼緵 Rokid/鎵嬫満璁块棶 Windows 涓绘帶鏈?IP銆?- Web demo 宸查€氳繃 Chrome 鎻掍欢瀹屾垚鍙褰╂帓锛屽苟鍦ㄦ湰杞噸鏂伴獙璇侊細鐐瑰嚮銆屽揩閫熷僵鎺掋€嶅悗鏈€缁堢姸鎬佷负 `User B`銆乣complete`銆佸啓鍥炵偣鏄剧ず銆屽悗鏉ヨ€呯湅瑙佷簡鏂颁俊鏍囥€嶏紝log 涓?`current_step` 涓?`write_back`銆?- 2026-07-02 11:24 Chrome 鎻掍欢澶嶉獙缁撴灉锛歚/api/state` 鏄剧ず `active_user=B`銆乣mission_state=complete`銆乣current_step_index=3`銆乣completed_steps=[read, find_year, service_action, write_back]`銆乣beacons=3`锛涙渶鏂板啓鍥炰俊鏍囦负 `B_WRITE_1782962678108`锛岄敋鐐?`A3`锛屽啓鍥炴椂闂?`2026-07-02T03:24:38.108Z`銆?- Space Server 宸叉敮鎸?CORS銆乣OPTIONS` preflight銆乣/api/health`銆乶o-store JSON锛屽苟閫氳繃 `npm run check:unity`銆?- Unity shell 鏀寔鍙厤缃?API 鍦板潃锛氬懡浠よ `--innerworld-api=...`銆佺幆澧冨彉閲?`INNERWORLD_API_BASE_URL`銆佹寔涔呭寲閰嶇疆銆乣StreamingAssets`銆乣Resources` 鍖呭唴閰嶇疆閮藉彲鐢ㄣ€?- Windows fallback 宸查噸寤猴細`output\unity-windows\InnerWorldRokid.exe`锛屽苟澶嶅埗 `InnerWorldRokid_Data\StreamingAssets\innerworld-config.json`銆?- Windows Unity fallback 宸茬敤 Windows 搴旂敤鎺у埗瀹炴満绐楀彛楠屾敹锛欻UD 鏄剧ず `LOCALHOST LIVE`锛孉1/A2/A3 涓夐敋鐐瑰彲瑙侊紝鐑敭鎻愮ず鍙锛涙寜 `Space, Space, S, W, B, R` 鍚庣獥鍙ｅ拰 API 閮借繘鍏?`active_user=B`銆乣mission_state=complete`銆乣completed_steps=4`銆乣beacon_count=3`銆?- 淇 Unity Player 绮夊/榛戝満闂锛氫笉鍐嶇敤 `Shader.Find("Standard")` 鐩存帴鍒涘缓杩愯鏃舵潗璐紝鏀逛负璺?Built-in/URP 鐨勫彲鐢?shader 鏌ユ壘鍜?renderer 鐜版湁鏉愯川鍏滃簳锛汸layer.log 宸叉棤鏉愯川绌哄紩鐢ㄥ紓甯搞€?- 淇 Space Server 瀹屾垚鎬佸綊涓€锛氭墍鏈変换鍔″畬鎴愭垨鍖呭惈 `write_back` 鏃跺己鍒?`mission_state=complete`锛岄伩鍏嶅鎴风鍒?User B 鏃舵妸鐘舵€侀檷鍥?`entered`銆?- Android fallback APK 宸查噸寤猴細`output\unity-android\InnerWorldRokid.apk`锛岀敓鎴愭椂闂?`2026-07-02 11:19:30`锛孲HA256 `D507E209A245CB663FF775E85B098AF159DE977E642B45606DA1F8BE35285574`銆?- Android APK 宸茬敤 `aapt` 楠岃瘉锛歮anifest 涓寘鍚?`android.permission.INTERNET`銆乣android:usesCleartextTraffic=true`銆乣android:networkSecurityConfig=@xml/...`銆?- Android 閰嶇疆鍏滃簳宸查獙璇侊細APK 鍐呭寘鍚?`assets/innerworld-config.json`锛孶nity `Resources` 閰嶇疆涔熻鎵撳叆 `assets/bin/Data/cb7d38ef34a54f68b7f0196e2b07d22a`銆?- `tools/set-unity-config.ps1` 浼氬悓鏃舵洿鏂?`Assets\StreamingAssets\innerworld-config.json` 鍜?`Assets\Resources\innerworld-config.json`锛岄伩鍏?LAN IP 閰嶇疆鍒嗗弶銆?- 浜や粯鍖呭凡閲嶆柊鐢熸垚骞堕獙鍖咃細鍖呭惈 Android 鎻掍欢銆丷esources 閰嶇疆銆丼treamingAssets 閰嶇疆銆乄indows fallback銆丄ndroid APK銆佹枃妗ｅ拰缂撳瓨鎶ュ憡锛岀鍏ョ洰褰曡鏁颁负 0銆?- 鏂板 `npm run evidence:rehearsal`锛氳嚜鍔ㄨ蛋瀹?reset銆佽鍙栥€佸鎵惧勾浠姐€佸姞鍏ユ椿鍔ㄣ€佸啓鍥炪€佸垏 User B锛屽苟鎶?JSON/Markdown 楠屾敹璇佹嵁鍐欏叆 `output/demo`銆傚甫 `-- --reset-after` 鏃朵繚瀛樿瘉鎹悗鎭㈠鍒濆鎬併€?- `npm run evidence:rehearsal -- --reset-after` 宸查獙璇侀€氳繃锛屾渶鏂拌瘉鎹啓鍏?`output\demo\rehearsal-evidence-latest.json` / `.md`锛氭渶缁堟€佷负 `active_user=B`銆乣mission_state=complete`銆乣completed_steps=4`銆乣beacon_count=3`锛岄殢鍚?reset 鍥?`entered`銆? 涓俊鏍囥€? 涓畬鎴愭楠ゃ€?- 鎵撳寘鑴氭湰宸叉帓闄?`data/runtime_state.json`锛岄伩鍏嶆妸 Chrome/Unity 褰╂帓鍚庣殑 `complete` 杩愯鎬佸甫杩涗氦浠樺寘锛涜В鍖呰繍琛岀儫娴嬪凡楠岃瘉锛氬寘鍐?server 棣栨鍚姩浼氳嚜鍔ㄧ敓鎴愯繍琛屾€侊紝`/api/health` 杩斿洖 `entered`銆? 涓俊鏍囥€? 涓畬鎴愭楠ゃ€?- 鏂板骞堕獙璇?`npm run pdf:fieldkit`锛氶€氳繃 sibling `pdf-renderer` 鐨?Java/OpenHTMLToPDF 娓叉煋鍙墦鍗扮幇鍦哄寘 `output\pdf\rokid_innerworld_field_kit.pdf`锛屽唴瀹瑰寘鍚叆鍙?QR銆佽澶囬〉銆?0 绉掓祦绋嬨€丄1/A2/A3 閿氱偣鐗屻€佹搷浣滃憳娓呭崟鍜岃嚜鍔ㄥ僵鎺掕瘉鎹憳瑕併€俙pdfinfo` 鏄剧ず 7 椤?A4锛涘凡鐢?Poppler 娓叉煋 PNG 鐩灏侀潰銆佸惎鍔ㄩ〉銆侀敋鐐圭墝鍜屾搷浣滃憳椤碉紝鏃犳枃瀛楅噸鍙犳垨涓枃缂哄瓧銆?- 鏂板 `npm run server:package` 鍜?`docs\server-deploy.md`锛氱敓鎴愯交閲?Space Server 鍙戝竷鍖咃紝鍖呭惈 Node server銆乄eb demo銆乣space_demo.json`銆丄I schema/prompt銆佹枃妗ｅ拰 Windows/Linux 鍚姩鑴氭湰锛涙槑纭帓闄?`data/runtime_state.json`銆乁nity 杈撳嚭銆乣node_modules`銆乣.git` 鍜岀紦瀛橈紝鐢ㄤ簬鍏堟湰鍦拌В鍖呯儫娴嬶紝鍐嶄笂浼犲皬涓绘満銆?- 鏂板 `npm run server:smoke`銆乣npm run package:audit`銆乣npm run verify:release`锛氭妸 server-only 瑙ｅ寘鐑熸祴銆佷富鍖?鍐呭祵 server release SHA 鏍￠獙銆佸繀闇€鏂囦欢妫€鏌ュ拰绂佸叆鐩綍妫€鏌ュ浐鍖栦负鑴氭湰锛屽噺灏戠幇鍦哄墠鎵嬪伐楠屽寘澶辫銆?- 鏂板 `npm run server:deploy-plan`锛氭牴鎹渶鏂?server-only zip/manifest 鐢熸垚 `output\server-release\deploy-plan-latest.md` / `.json`锛屽寘鍚笂浼犺矾寰勩€丩inux 鍚姩銆乻ystemd銆丆addy 鍜?Nginx 鍙嶄唬绀轰緥锛涘彧鍐欐湰鍦拌鍒掞紝涓嶈繛鎺ユ湇鍔″櫒銆?- 鏂板 `npm run env:doctor`锛氭眹鎬?Node/npm/Java/Maven/Unity/Android 妯″潡銆佺鐩樸€佺鍙ｃ€丄PI health銆乄indows EXE銆丄ndroid APK銆丳DF銆佹渶鏂颁富鍖呭拰 server-only 鍖咃紝璇佹嵁鍐欏叆 `output\env-doctor`锛屽苟鎺ュ叆 `verify:release` 鍜屼富鍖呭璁°€?- 鏂板 `npm run field:preflight`锛氳嚜鍔ㄨ瘑鍒?LAN IP銆佹鏌?localhost/LAN health銆佹洿鏂?Unity API 鍦板潃銆佹寜 LAN URL 閲嶆覆鏌撶幇鍦?PDF锛屽苟鎶婄幇鍦洪妫€璇佹嵁鍐欏叆 `output\field-preflight`锛汱AN 涓嶉€氭椂榛樿鎷掔粷鏇存柊 Unity/PDF锛屾寮忕幇鍦哄彲鐢?`-RequireLan` 寮烘牎楠岋紝涓诲寘瀹¤浼氭鏌ヨ鑴氭湰鍜?latest 鎶ュ憡瀛樺湪銆?- 鏂板 `npm run release:index`锛氭眹鎬绘渶鏂颁富鍖呫€乻erver-only 鍖呫€丒XE/APK/PDF銆佺幆澧冨尰鐢熴€佺幇鍦洪妫€鍜屽僵鎺掕瘉鎹紝杈撳嚭 `output\release-index\release-index-latest.md` / `.json`锛沗verify:release` 鍦ㄤ富鍖呭璁″拰 post-package 鐜鍖荤敓鍚庤嚜鍔ㄨ繍琛屻€?- `tools\render-field-kit-pdf.ps1` 宸叉敼涓轰紭鍏堜娇鐢?`D:\Downloads\RokidCache\m2-repository` 浣滀负 Maven 鏈湴浠撳簱锛沗cache:report` 鏂板 Maven C 鐩樹粨搴撱€乄indows Temp 鎬婚噺鍜?D Maven cache 鎶ュ憡椤癸紝渚夸簬鎸佺画瑙傚療 C 鐩橀闄┿€?- 鏂板骞惰瘯杩愯 `npm run cache:temp:report` / `npm run cache:temp:clean`锛氬彧澶勭悊鐢ㄦ埛 Temp 涓?6 灏忔椂浠ュ墠銆佸彲璇嗗埆涓?VS/Android/.NET 瀹夎鍣ㄦ畫鐣欎笖鏃犺繘绋嬪紩鐢ㄧ殑鐩綍锛屽苟鏄惧紡璺宠繃 junction/symlink 绛?reparse point锛涙湰杞垹闄?`glgyitip`銆乣15tbxetl`銆乣b54aud4l.eiq`锛岄噴鏀剧害 7.416GB銆?- C 鐩樹粛鎺ヨ繎 25GB 璀︽垝绾匡紝娓呯悊鍚庣害 23.741GB 鍙敤锛涚户缁珮棰戞墽琛?`npm run cache:temp:report`銆乣npm run cache:report` / `npm run cache:clean`銆備笉闅忔剰鍒犻櫎 Unity `Library`銆?

## 2026-07-05 - Visible Page, Missing Real Input

- User field report: the real-device page can display, but the virtual remote and hand are absent, leaving no usable interaction path. This is a P0 physical acceptance blocker, not a display success.
- Unity now includes a bottom `Field Input Assist Rail` with A1/A2/A3, Confirm, Service, Write, User B, and Reload plus keyboard mappings 1/2/3, Space/Enter, S/W/B/R/C. This keeps鐜板満 rehearsal and debugging moving while the true RKInput/PointableUI/hand path is repaired.
- Heartbeat payloads now mark this path as fallback/operator assist with `fallback_input_visible`, `operator_assist_input`, `input_blocker=visible_but_no_remote_or_hand`, and `input_acceptance_mode=operator_assist_rehearsal_not_hardware_ready`; these fields must not be used to claim hardware-ready.
- Spatial shell upgrade started: A1/A2/A3 use depth offsets and a curved Spatial Memory Depth Ribbon, reducing the flat-panel feel and making the P0 loop read as a spatial memory field.
- Latest short field acceptance smoke: `output/field-acceptance-session/field-acceptance-session-20260705-123001.json`; expected `ok=false` / `hardware_ready_claim_allowed=false` until trusted live input, A1/A2/A3, A3 write-back, and User B readback are complete.

## 2026-07-05 - Station Pro ADB Field Input Assist

- Added `tools/station-pro-field-input-assist.ps1` and npm scripts `station:field-input-assist`, `station:field-input-assist:p0`, and `station:field-input-assist:apply` so Windows can drive the visible Unity field assist through Station Pro ADB keyevents when the virtual remote/hand UI is absent.
- Default mode is dry-run evidence only; real keyevents require explicit `-Apply -RequireDevice`. Reports write sanitized transport/hash evidence and never include raw device IDs, private IPs, pairing codes, logcat, or dumpsys.
- P0 key mapping is constrained to A1, Confirm, A2, Confirm, Service, A3, Write, User B. It is tagged `operator_assist_rehearsal_not_hardware_ready` with blocker `visible_but_no_remote_or_hand`, so it cannot satisfy hardware-ready gates.
- Latest dry-run evidence: `output/station-pro-field-input-assist/station-pro-field-input-assist-latest.json` showed ADB found, one USB device-state transport selected by hash prefix, `apply_requested=false`, and no sent keyevents.

## 2026-07-05 - Field Session Captures Input Assist

- `field:acceptance-session` now runs Station Pro field input assist in dry-run mode by default and records `station_input_assist` in the same session JSON/Markdown as operator-plan, glasses diagnostics, live-pass, and target-pass.
- New session switches: `-SkipInputAssist` to omit it, `-ApplyInputAssist` to send rehearsal keyevents through ADB with `-RequireDevice`. Even when applied, the session report keeps `hardware_acceptance_evidence=false` and `hardware_ready_claim_allowed=false`.
- Latest smoke: `output/field-acceptance-session/field-acceptance-session-20260705-125426.json` showed `station_input_assist.command_ok=true`, `apply_requested=false`, `input_blocker=visible_but_no_remote_or_hand`, `sent_step_count=0`, and a next action reminding that `station:field-input-assist:apply` is rehearsal only.

## 2026-07-05 - Input Assist Space API Readback

- `station-pro-field-input-assist.ps1` now reads `/api/state` and `/api/device/sessions` before and after dry-run/apply. It records mission state, active user, completed steps, beacon counts, write-back beacon count, session counts, latest active anchor, latest input command, and sanitized privacy booleans without raw session IDs or raw device IDs.
- The readback has an explicit `proves_hardware_input=false` guard. It is useful to verify whether ADB keyevents correlate with Space API summaries, but it still cannot replace RKInput/PointableUI/hand evidence.
- `field:acceptance-session` now lifts the input assist readback into `station_input_assist`: `readback_state_ok`, `readback_sessions_ok`, latest active anchor, mission state, deltas, and `readback_proves_hardware_input=false`.
- Latest smoke with localhost server up: `output/field-acceptance-session/field-acceptance-session-20260705-130413.json` had `readback_state_ok=true`, `readback_sessions_ok=true`, `readback_mission_state=entered`, `readback_proves_hardware_input=false`, and `hardware_acceptance_evidence=false`.
## Next

- 閲嶆柊鐢熸垚鏈€缁堜氦浠樺寘骞堕獙鍖咃紝纭鏈嶅姟鍣ㄩ儴缃茶鍒掋€佺幆澧冨尰鐢熴€乺elease index銆佺幇鍦?LAN 棰勬鑴氭湰銆乄indows/Android fallback銆丼pace Server 鐘舵€佹満淇鍜屾枃妗ｅ叏閮ㄨ繘鍏?zip銆?- 纭欢鍒板満鍚庯紝鎶?Rokid AR Studio 杈撳叆/鏄剧ず灞傛帴鍒板悓涓€濂?Space API 鍜屾暟鎹绾︺€?
